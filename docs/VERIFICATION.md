# Verification report

Generated by `python -m verify report`. Every figure here is measured on the machine that ran it — timings will differ on yours, and the conclusions drawn from them should not.

## Complexity

105 subjects. **5** expected, **8** neighbour, **92** match.

`MATCH` means the measured growth curve is the one the implementation claims. `NEIGHBOUR` covers the pairs measurement cannot separate over a couple of decades of n — O(n) against O(n log n) differ by a factor of log n, which is 6 to 14 across the sizes used here and sits inside the noise as often as outside it. `EXPECTED` marks claims that are correct under the textbook assumption of O(1) arithmetic and wrong in Python.

| subject | declared | measured | confidence | status | scaling |
| --- | --- | --- | ---: | --- | --- |
| `catalan` | O(n^2) | O(n^3) | 20.4× | EXPECTED | the index; declared O(n^2) assuming O(1) multiplication |
| `climb_stairs` | O(n) | O(n^2) | 3.7× | EXPECTED | step count; result is a Fibonacci number, so digits grow with n |
| `factorial` | O(n) | O(n^2) | 4.0× | EXPECTED | the value; declared O(n) assuming O(1) multiplication |
| `fib` | O(log n) | O(n^2) | 1.5× | EXPECTED | the index; declared O(log n) assuming O(1) arithmetic |
| `product_except_self` | O(n) | O(n^3) | 1.6× | EXPECTED | list length; the accumulated product grows without bound |
| `batches` | O(n log n) | O(n) | 1.6× | NEIGHBOUR | vertex count; levels are sorted |
| `dedupe` | O(n) | O(n log n) | 1.2× | NEIGHBOUR | list length |
| `is_anagram` | O(n) | O(n log n) | 1.1× | NEIGHBOUR | string length |
| `jaccard` | O(n) | O(n log n) | 1.9× | NEIGHBOUR | set size |
| `min_heap_push` | O(n log n) | O(n) | 1.1× | NEIGHBOUR | push count |
| `popcount` | O(log n) | O(1) | 1.0× | NEIGHBOUR | the value; work is proportional to set bits |
| `quickselect` | O(n) | O(n log n) | 1.2× | NEIGHBOUR | list length, k at the median |
| `two_sum` | O(n) | O(n log n) | 1.1× | NEIGHBOUR | list length |
| `bellman_ford` | O(n^2) | O(n^2) | 1.3× | MATCH | vertex count with 2n edges |
| `bfs` | O(n) | O(n) | 1.3× | MATCH | vertex count, out-degree fixed at 3 |
| `binary_gap` | O(log n) | O(log n) | 2.0× | MATCH | the value itself |
| `binary_search` | O(log n) | O(log n) | 1.6× | MATCH | sorted list length |
| `bloom_add` | O(n) | O(n) | 16.3× | MATCH | insertion count |
| `bst_insert` | O(n log n) | O(n log n) | 7.1× | MATCH | insertion count |
| `bubble_sort` | O(n^2) | O(n^2) | 2.4× | MATCH | list length |
| `caesar` | O(n) | O(n) | 7.1× | MATCH | string length |
| `chunk` | O(n) | O(n) | 4.1× | MATCH | list length |
| `coin_change` | O(n) | O(n) | 2.2× | MATCH | target amount, coin set fixed |
| `components` | O(n log n) | O(n log n) | 1.0× | MATCH | vertex count; groups are sorted |
| `counting_sort` | O(n) | O(n) | 7.6× | MATCH | list length, values bounded by n |
| `dfs` | O(n) | O(n) | 1.2× | MATCH | vertex count, out-degree fixed at 3 |
| `digital_root` | O(1) | O(1) | 1.0× | MATCH | the value itself |
| `dijkstra` | O(n log n) | O(n log n) | 4.7× | MATCH | vertex count, out-degree fixed at 3 |
| `fenwick_add` | O(n log n) | O(n log n) | 24.9× | MATCH | update count |
| `find_peak` | O(log n) | O(log n) | 4.0× | MATCH | list length |
| `first_true` | O(log n) | O(log n) | 7.5× | MATCH | search range |
| `flatten` | O(n) | O(n) | 35.3× | MATCH | leaf count |
| `flatten_dict` | O(n) | O(n) | 6.3× | MATCH | leaf count |
| `floyd_warshall` | O(n^3) | O(n^3) | 29.3× | MATCH | vertex count |
| `frequency_counter` | O(n) | O(n) | 2.5× | MATCH | item count |
| `gcd` | O(log n) | O(log n) | 2.8× | MATCH | operand magnitude |
| `gray_code` | O(2^n) | O(2^n) | 233.3× | MATCH | bit count |
| `group_anagrams` | O(n) | O(n) | 1.8× | MATCH | word count, each word fixed at 6 characters |
| `group_by` | O(n) | O(n) | 8.0× | MATCH | item count |
| `hamming` | O(n) | O(n) | 41.5× | MATCH | string length |
| `heap_sort` | O(n log n) | O(n log n) | 5.4× | MATCH | list length |
| `insertion_sort` | O(n^2) | O(n^2) | 2.5× | MATCH | list length |
| `is_balanced` | O(n) | O(n) | 4.8× | MATCH | string length, properly nested so there is no early exit |
| `is_bipartite` | O(n) | O(n) | 1.4× | MATCH | vertex count, actually bipartite so the whole graph is explored |
| `is_isomorphic` | O(n) | O(n) | 5.9× | MATCH | string length, genuinely isomorphic so there is no early exit |
| `is_palindrome` | O(n) | O(n) | 1.2× | MATCH | string length |
| `is_power_of_two` | O(1) | O(1) | 1.0× | MATCH | the value itself |
| `isqrt` | O(log n) | O(log n) | 9.3× | MATCH | the value itself |
| `kmp_search` | O(n) | O(n) | 12.6× | MATCH | text length, pattern fixed at 8 |
| `knapsack` | O(n^2) | O(n^2) | 3.0× | MATCH | item count and capacity both n |
| `lcs_length` | O(n^2) | O(n^2) | 10.8× | MATCH | both strings of length n |
| `levenshtein` | O(n^2) | O(n^2) | 3.6× | MATCH | both strings of length n |
| `lis_length` | O(n log n) | O(n log n) | 2.0× | MATCH | list length |
| `longest_consecutive` | O(n) | O(n) | 1.5× | MATCH | list length |
| `longest_palindrome` | O(n) | O(n) | 1.4× | MATCH | random text; centres collapse immediately, so this is average case |
| `longest_palindrome_worst` | O(n^2) | O(n^2) | 2.6× | MATCH | a single repeated character; every centre expands fully |
| `longest_unique` | O(n) | O(n) | 5.9× | MATCH | string length |
| `lower_bound` | O(log n) | O(log n) | 4.5× | MATCH | sorted list length |
| `lps_length` | O(n^2) | O(n^2) | 4.9× | MATCH | string length |
| `lru_put` | O(n) | O(n) | 1.8× | MATCH | insertion count |
| `luhn_valid` | O(n) | O(n) | 31.7× | MATCH | digit count |
| `majority` | O(n) | O(n) | 9.1× | MATCH | list length |
| `matrix_chain` | O(n^3) | O(n^3) | 40.8× | MATCH | matrix count |
| `max_area` | O(n) | O(n) | 18.8× | MATCH | list length |
| `max_subarray` | O(n) | O(n) | 3.0× | MATCH | list length |
| `merge_intervals` | O(n log n) | O(n log n) | 4.5× | MATCH | interval count |
| `merge_sort` | O(n log n) | O(n log n) | 9.3× | MATCH | list length |
| `min_path_sum` | O(n^2) | O(n^2) | 5.9× | MATCH | grid side |
| `min_window` | O(n) | O(n) | 5.4× | MATCH | text length, pattern fixed |
| `move_zeroes` | O(n) | O(n) | 11.6× | MATCH | list length |
| `moving_average` | O(n) | O(n) | 8.1× | MATCH | list length, window fixed |
| `natural_key` | O(n) | O(n) | 8.9× | MATCH | string length |
| `normalise_whitespace` | O(n) | O(n) | 6.7× | MATCH | string length |
| `pascal` | O(n^2) | O(n^2) | 1.7× | MATCH | row count |
| `power_mod` | O(log n) | O(log n) | 9.2× | MATCH | exponent magnitude |
| `prefix_sum_build` | O(n) | O(n) | 1.2× | MATCH | list length |
| `primes_below` | O(n) | O(n) | 14.0× | MATCH | the limit; true cost is n log log n, which no model here expresses |
| `quick_sort` | O(n log n) | O(n log n) | 5.8× | MATCH | list length |
| `rabin_karp` | O(n) | O(n) | 8.8× | MATCH | text length, pattern fixed at 8 |
| `radix_sort` | O(n) | O(n) | 1.4× | MATCH | list length, values below 10^6 so passes are constant |
| `reverse_bits` | O(1) | O(1) | 1.0× | MATCH | value; width fixed at 32 so the loop count is constant |
| `reverse_words` | O(n) | O(n) | 1.2× | MATCH | string length |
| `rle_encode` | O(n) | O(n) | 2.9× | MATCH | string length |
| `rob` | O(n) | O(n) | 7.2× | MATCH | list length |
| `rotate` | O(n) | O(n) | 2.7× | MATCH | list length |
| `runs` | O(n) | O(n) | 3.9× | MATCH | list length |
| `search_rotated` | O(log n) | O(log n) | 1.8× | MATCH | rotated sorted list length |
| `segment_tree_build` | O(n) | O(n) | 2.9× | MATCH | value count |
| `selection_sort` | O(n^2) | O(n^2) | 11.5× | MATCH | list length |
| `similarity` | O(n^2) | O(n^2) | 3.0× | MATCH | both strings of length n |
| `single_number` | O(n) | O(n) | 14.8× | MATCH | list length |
| `subsets` | O(2^n) | O(2^n) | 49.5× | MATCH | element count |
| `ternary_search` | O(1) | O(1) | 1.0× | MATCH | interval width; iteration count is fixed at 200 regardless |
| `three_way_partition` | O(n) | O(n) | 8.5× | MATCH | list length |
| `to_base` | O(log n) | O(log n) | 1.7× | MATCH | the value itself |
| `topological_sort` | O(n log n) | O(n log n) | 1.6× | MATCH | vertex count; the initial queue is sorted, hence n log n |
| `transpose` | O(n^2) | O(n^2) | 4.4× | MATCH | square matrix of side n |
| `trap` | O(n) | O(n) | 21.5× | MATCH | list length |
| `trie_insert` | O(n) | O(n) | 1.4× | MATCH | word count |
| `unique_paths` | O(n^2) | O(n^2) | 1.1× | MATCH | grid side |
| `upper_bound` | O(log n) | O(log n) | 4.3× | MATCH | sorted list length |
| `window_max` | O(n) | O(n) | 7.8× | MATCH | list length, window fixed at 16 |
| `word_break` | O(n^2) | O(n^2) | 7.3× | MATCH | string length |
| `word_frequency` | O(n) | O(n) | 2.9× | MATCH | string length |
| `z_function` | O(n) | O(n) | 1.5× | MATCH | string length |

### Where the textbook is wrong about Python

Complexity analysis assumes arithmetic is a constant-time operation. Python integers are arbitrary-precision, so addition costs time proportional to the number of digits and multiplication rather more. Any algorithm whose *result* grows with n therefore has a hidden factor the textbook analysis omits:

- **`product_except_self`** — declared O(n), measured O(n^3).
- **`factorial`** — declared O(n), measured O(n^2).
- **`fib`** — declared O(log n), measured O(n^2).
- **`catalan`** — declared O(n^2), measured O(n^3).
- **`climb_stairs`** — declared O(n), measured O(n^2).

`fib` is the sharpest case. Fast doubling really is O(log n) *operations*, but the n-th Fibonacci number has O(n) digits, so those O(log n) multiplications are each on enormous integers. The claim and the measurement are both right; they are counting different things.

## Properties

58 properties, 0 failing, 200 generated inputs each.

All properties hold. The interesting ones being asserted:

- every sort is **ordered**, a **permutation** of its input, **idempotent**, and **agrees with `sorted()`** — kept as four separate claims so a failure says which invariant broke
- `levenshtein` satisfies the three obligations of a **metric**: identity, symmetry, and the triangle inequality
- `lower_bound` **is** the insertion point: everything below it is strictly less, everything from it is greater or equal
- `fenwick_tree` and `segment_tree` agree with naive recomputation over **every** sub-range, not a sampled few
- round trips are involutions: `rle`, `roman`, `to_base`, `caesar`, `reverse_bits`

## Adversarial inputs

Exact comparison counts at n=64, collected by wrapping each element in a type that increments a counter when compared — no changes to the implementations and no profiler overhead. `searched` is the worst input found by hill-climbing from a random permutation, accepting any swap that costs more comparisons.

For reference: log2(64!) = **296** is the information-theoretic floor for any comparison sort, and n(n−1)/2 = **2016** is what a quadratic sort costs.

| sort | random | sorted | reversed | all_equal | organ_pipe | sawtooth | almost_sorted | searched | worst/random |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `merge_sort` | 300 | 192 | 192 | 192 | 223 | 276 | 239 | 321 | 1.07× |
| `quick_sort` | 634 | 527 | 571 | 130 | 883 | 368 | 673 | 814 | 1.39× |
| `insertion_sort` | 1156 | 63 | 2016 | 63 | 1055 | 847 | 120 | 1831 | 1.74× |
| `selection_sort` | 2016 | 2016 | 2016 | 2016 | 2016 | 2016 | 2016 | 2016 | 1.00× |
| `heap_sort` | 575 | 593 | 525 | 186 | 590 | 557 | 590 | 601 | 1.05× |
| `bubble_sort` | 1971 | 63 | 2016 | 63 | 2016 | 1925 | 1845 | 2016 | 1.02× |

What the table says:

- **`merge_sort` comes within 1.4% of the information-theoretic bound** on random input (300 comparisons against a floor of 296). There is almost nothing left to win.
- **`selection_sort` is input-oblivious**: identical count on every pattern, and hill-climbing finds nothing worse. It always performs exactly n(n−1)/2 comparisons, which makes it predictable and unimprovable in equal measure.
- **`insertion_sort` is adaptive**: 63 comparisons on sorted input against 2016 on reversed — a 32× spread driven entirely by input order.
- **`bubble_sort` is adaptive**: 63 comparisons on sorted input against 2016 on reversed — a 32× spread driven entirely by input order.
- Hill-climbing hurt **`insertion_sort`** most, finding an input 1.74× worse than random.
- For `merge_sort`, `heap_sort`, the search found inputs worse than **anything in the fixed battery**. That is the argument for searching rather than quoting known-bad patterns: it optimises against this implementation's specific decisions, not against sorting in general.

## Crossover points

Where the asymptotically better algorithm actually starts winning. A crossover is only recorded when the faster algorithm *stays* faster at every larger size measured — one crossing is noise.

### sorting

builtin_sorted is Timsort implemented in C. It is included as the floor: it shows how much of the gap between these implementations is algorithmic and how much is simply the interpreter.

| n | `insertion_sort` | `selection_sort` | `merge_sort` | `quick_sort` | `heap_sort` | `builtin_sorted` |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 4 | 0.5µs | 1.7µs | 1.6µs | 1.1µs | 1.0µs | 0.2µs |
| 8 | 1.0µs | 3.7µs | 3.7µs | 2.5µs | 2.7µs | 0.2µs |
| 16 | 2.8µs | 9.6µs | 8.8µs | 5.2µs | 6.7µs | 0.5µs |
| 32 | 10.7µs | 28.6µs | 19.8µs | 12.0µs | 16.5µs | 1.2µs |
| 64 | 36.4µs | 94.9µs | 44.0µs | 27.0µs | 38.2µs | 2.7µs |
| 128 | 151.1µs | 347.6µs | 97.1µs | 59.3µs | 91.0µs | 6.1µs |
| 256 | 590.0µs | 1276.8µs | 211.4µs | 130.2µs | 211.7µs | 13.7µs |
| 512 | 2775.7µs | 5940.7µs | 457.1µs | 321.6µs | 500.5µs | 29.5µs |
| 1024 | 12450.0µs | 24133.3µs | 1010.7µs | 751.7µs | 1158.1µs | 65.0µs |
| 2048 | 50780.9µs | 96795.2µs | 2212.4µs | 1667.0µs | 2612.2µs | 142.8µs |

- `merge_sort` overtakes `insertion_sort` at **n = 128**
- `quick_sort` overtakes `insertion_sort` at **n = 64**
- `heap_sort` overtakes `insertion_sort` at **n = 128**
- `merge_sort` overtakes `selection_sort` at **n = 16**
- `merge_sort` overtakes `heap_sort` at **n = 256**
- `quick_sort` overtakes `heap_sort` at **n = 8**

### median selection

Quickselect is O(n) against sorting's O(n log n), but sorting runs in C. The asymptotics and the measurement disagree over the whole practical range.

| n | `quickselect` | `sort_then_index` | `heapq_nsmallest` |
| ---: | ---: | ---: | ---: |
| 16 | 1.6µs | 0.6µs | 2.1µs |
| 64 | 5.0µs | 2.9µs | 8.1µs |
| 256 | 19.0µs | 13.6µs | 36.7µs |
| 1024 | 112.1µs | 71.6µs | 181.9µs |
| 4096 | 273.7µs | 309.5µs | 865.5µs |
| 16384 | 1722.0µs | 1500.0µs | 4138.8µs |

- `sort_then_index` overtakes `quickselect` at **n = 16384**

### membership

O(log n) against O(n), but `in` is a C loop while binary search is interpreted arithmetic. Each timing is 200 lookups at positions spread across the array, so this is average-case, not worst-case.

| n | `binary_search` | `linear_scan` | `python_in` |
| ---: | ---: | ---: | ---: |
| 8 | 37.0µs | 37.8µs | 11.7µs |
| 16 | 45.2µs | 48.2µs | 15.7µs |
| 32 | 55.6µs | 73.6µs | 23.3µs |
| 64 | 64.7µs | 124.1µs | 36.4µs |
| 128 | 74.4µs | 226.3µs | 64.4µs |
| 256 | 86.6µs | 465.5µs | 120.4µs |
| 1024 | 135.4µs | 2178.1µs | 449.0µs |
| 4096 | 178.4µs | 8872.5µs | 1813.7µs |

- `binary_search` overtakes `python_in` at **n = 256**

